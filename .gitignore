# Secretos y entorno — jamás versionar (PLAN.md §10)
.env
*.env
!.env.example
# Python
__pycache__/
*.pyc
.venv/
venv/
# Django
db.sqlite3
staticfiles/
media/
# SO / editor
.DS_Store
Thumbs.db

# Herramientas y config local de Claude Code (no del proyecto)
.claude/
# Salida generada del grafo de conocimiento (graphify)
graphify-out/
# Entregables generados para el contador (PDF/HTML locales)
entregables-contador/
