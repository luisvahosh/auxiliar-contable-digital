Django>=5.2,<6
python-dotenv>=1.0
dj-database-url>=2.1
defusedxml>=0.7  # parseo seguro del XML DIAN (CLAUDE.md §4: nunca xml.etree directo)
requests>=2.31   # API de Alegra
argon2-cffi>=23  # hash de contraseñas Argon2 (PLAN.md §12)
django-axes>=6.1 # rate limiting de login por IP y cuenta (PLAN.md §12)
pypdf>=4         # texto de extractos bancarios en PDF (P4.5)
gunicorn>=22     # servidor WSGI de producción (contenedor)
whitenoise>=6.6  # estáticos servidos por la app (sin nginx aparte)
django-otp>=1.5  # segundo factor TOTP (PLAN.md §12)
qrcode>=7.4      # QR de aprovisionamiento 2FA (SVG, sin pillow)
pillow>=10       # preprocesar fotos de facturas (rotación EXIF, tamaño) P1.10
cryptography>=42 # tokens de conexiones contables cifrados en reposo (PLAN §10)
psycopg[binary]>=3.2  # driver PostgreSQL en producción (sqlite sigue por defecto en local)
openpyxl>=3.1    # leer el PUC de la empresa exportado en Excel (.xlsx) por su software contable
